import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Acheeve-Ionic-Midterms',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
